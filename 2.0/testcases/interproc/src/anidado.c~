#include <stdio.h> 
#include <stdlib.h>

int suma(int a, int b){         
	return resta(a, -1 * b);
}

int resta(int a, int b){
	return a - b;
}

int main(int argc, char *argv[]) {
	int x;
	int result = suma(x,2);
	return result;
}
